[build]
# Use the modern Railpack builder
builder = "railpack"

[deploy]
# This defines the daily schedule (12:00 PM UTC)
cronSchedule = "0 12 * * *"

# Important: Cron jobs must NOT restart automatically
restartPolicyType = "never"

# The command to execute your script
startCommand = "python main.py"